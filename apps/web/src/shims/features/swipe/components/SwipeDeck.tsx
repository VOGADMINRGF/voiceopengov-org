"use client";
import React from "react";
export default function SwipeDeck(_: { userHash?: any }) {
  return (
    <div data-shim="SwipeDeck" className="border rounded p-3">
      SwipeDeck (shim)
    </div>
  );
}
